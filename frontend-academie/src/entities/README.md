# `entities/`

`entities/` contains domain contracts shared by multiple features.

Examples:

- user session contracts
- course contracts
- submission contracts

Keep this layer simple, reusable and independent from page rendering.
