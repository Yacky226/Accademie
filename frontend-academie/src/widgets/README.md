# `widgets/`

`widgets/` is optional.

Use it only for reusable sections that compose several features into one block.
If a block belongs to only one feature, keep it inside that feature instead.

Current status:

- no active widgets are registered yet
- keep this directory empty until a real cross-feature composition appears
- do not create placeholder widget folders ahead of actual usage
